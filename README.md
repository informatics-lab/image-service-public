# Image Service
Converts atmos sci data to encoded images. For use encoding 3D data arrays as a 2D image which can then be ingested into WebGL as a texture.

## Installing dependencies
    pip install -r requirements.txt